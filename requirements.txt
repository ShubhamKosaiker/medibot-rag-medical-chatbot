# ── Core LLM & LangChain ──────────────────────────────────────────────────────
langchain==0.1.20
langchain-community==0.0.38
langchain-huggingface==0.0.3
langchain-groq==1.1.2          # Groq LLM integration for LangChain (>=0.2 fixes proxies error)
langchain-text-splitters==0.0.2

# ── LLM Providers & Embeddings ────────────────────────────────────────────────
groq==0.9.0                    # Groq Python SDK (used by langchain-groq)
huggingface_hub==0.24.6
transformers==4.41.2
sentence-transformers==2.7.0   # MiniLM-L6-v2 embedding model

# ── Vector Store ──────────────────────────────────────────────────────────────
faiss-cpu==1.8.0               # Local FAISS vector store (CPU version)

# ── Document Loaders ──────────────────────────────────────────────────────────
pypdf==4.2.0                   # PDF loading and parsing
wikipedia==1.4.0               # Wikipedia article loader (used in build_vectorstore.py)
beautifulsoup4>=4.12.0         # Required by WebBaseLoader for web page parsing

# ── Web UI ────────────────────────────────────────────────────────────────────
streamlit==1.35.0              # Streamlit UI framework

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1           # Load secrets from .env file
tqdm==4.66.4
numpy==1.24.4
torch==2.1.0                   # Required by sentence-transformers
